# Onekey — Node.js (JavaScript) example

```sh
npm install openai
export ONEKEY_KEY="ok-your-key"
export GATEWAY_URL="http://localhost:8000"
node chat.mjs
```

Uses the official OpenAI SDK with a custom `baseURL` pointing at your gateway.
